package jdbc;

public class Review {
	/*
	 * in null is not null 커미션 null이 아닌것을 출력
	 * 이름순으로 출력
	 * 봉급은 몇이상 
	 * group by
	 * 도시별 근무사원수
	 * nvl 부서없음 커미션을 받지않는 사람을 제외하고 사원의 이름, 부서명을 출력하는데 부서가
	 * 없으면 부서없음으로 출력하시오
	 * 중복없이 무슨 부서가 있는지 distinct 위치는 칼럼명 앞에
	 * count(*)와 count(salary)의 차이점 ->null값 포함 유무
	 * 
	 */
}
